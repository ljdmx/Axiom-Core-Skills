{
    "snippets": {
        "auth_header_helper": {
            "desc": "Standard Bearer token extraction",
            "code": "const token = req.headers.authorization?.split(' ')[1];",
            "usage_count": 0,
            "category": "backend"
        },
        "safe_async_handler": {
            "desc": "Async error wrapper for Express",
            "code": "const asyncHandler = fn => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next);",
            "usage_count": 0,
            "category": "backend"
        },
        "cn_utility": {
            "desc": "Tailwind class merger",
            "code": "import { clsx, type ClassValue } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs: ClassValue[]) {\n  return twMerge(clsx(inputs));\n}",
            "usage_count": 0,
            "category": "frontend"
        }
    }
}
